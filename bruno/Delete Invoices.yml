info:
  name: Delete Invoices
  type: http

http:
  method: DELETE
  url: http://localhost:8080/invoices
  headers:
    - name: X-API-Key
      value: demo-key-1234
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
